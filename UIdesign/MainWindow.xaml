<Window x:Name="window" x:Class="UIdesign.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIdesign"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1120" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ListItemstyle" TargetType="ListViewItem">
            <EventSetter Event="MouseDoubleClick" Handler="SListView_ItemDoubleClick"/>
        </Style>
        <Style x:Key="Dwn_ctl_Btn" TargetType="Button">
            <EventSetter Event="MouseDoubleClick" Handler="Dwn_ctl_DoubleClick"/>
        </Style>
        <Style x:Key="Dwn_del_Btn" TargetType="Button">
            <EventSetter Event="MouseDoubleClick" Handler="Dwn_del_DoubleClick"/>
        </Style>
        <local:Student x:Key="stu"/>
        <local:Fiction x:Key="fic"/>
        <local:ProgressBarvalue x:Key="Bar"/>
        
    </Window.Resources>
    <Grid x:Name="Total_grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="47*"/>
            <RowDefinition Height="58*"/>
            <RowDefinition Height="407*"/>
            <RowDefinition Height="48*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TabControl SelectionChanged="TabControl_SelectionChanged" Grid.Row="0" Grid.RowSpan="4" Grid.Column="0" Grid.ColumnSpan="5">

            <TabItem x:Name="Homepg_item" Header="首页" IsSelected="True" FontSize="24" Width="255" >
                <StackPanel >
                    <WrapPanel Height="72" Margin="0,0,0.333,0" Background="White" >
                        <Label Content="C#R" VerticalAlignment="Bottom" Width="126" Height="72"/>
                        <TextBox x:Name="keySearch" Width="816" FontStyle="Italic" VerticalAlignment="Bottom" Height="73" FontSize="20" BorderThickness="2" BorderBrush="Black"  Background="#FFEBEBEB" TextChanged="TextBox_TextChanged">
                            <TextBox.Resources>
                                <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left" >
                                    <VisualBrush.Visual>
                                        <TextBlock FontStyle="Italic" Text="请输入关键词" />
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </TextBox.Resources>
                        </TextBox>
                        <Button x:Name="searchBtn" Style="{StaticResource ButtonPrimary}" Width="156" Content="搜索" Height="72" FontSize="20" Click="Button_Click"/>
                    </WrapPanel>
                    <!--<hc:LoadingCircle Width="60" Height="60"  Visibility="{Binding ShowProgress}" Margin="399,0,398.6,0"/>-->
                    <ListView x:Name="Lv_HomePage" 
                              Background="#FFF1ECEC" 
                              SelectionChanged="ListView_SelectionChanged" 
                              ItemsSource="{Binding Source={StaticResource fic}}"
                              ItemContainerStyle="{StaticResource ListItemstyle}"
                              GridViewColumnHeader.Click="Sort_Click"
                              Grid.Row="2" Height="618" Margin="0,0,-0.667,0" RenderTransformOrigin="0.5,0.489"
                              >
                        <ListView.View >
                            <GridView hc:BackgroundSwitchElement.MouseDownBackground="Black">
                                <GridViewColumn x:Name="fiction_id" DisplayMemberBinding=
                          "{Binding Path=col_fiction_id}"  Header="序号" Width="180"></GridViewColumn>
                                <GridViewColumn x:Name="fiction_name" DisplayMemberBinding=
                          "{Binding Path=col_fiction_name}" Header="小说名称" Width="200"></GridViewColumn>
                                <GridViewColumn x:Name="author_name" DisplayMemberBinding=
                          "{Binding Path=col_fiction_author}" Header="作者" Width="200"></GridViewColumn>

                            </GridView>
                        </ListView.View>
                        <ListView.ContextMenu >
                            <ContextMenu Visibility="{Binding Conmenu}">
                                <MenuItem Header="查看详情" Click="InfoPage"/>
                                <MenuItem Header="添加书架" Click="BookShelf"/>
                                <MenuItem Header="下载本书" Click="DownLoadBook"/>
                            </ContextMenu>
                        </ListView.ContextMenu>
                    </ListView>
                </StackPanel>
            </TabItem>
            <TabItem Header="下载" IsSelected="True" FontSize="24" Width="255">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="113*"/>
                    </Grid.ColumnDefinitions>
                    <TabControl Margin="0,0,-0.2,0">
                        <TabItem Header="正在下载" Name="Dwning_btn" IsSelected="True" FontSize="24" Width="200">
                            <StackPanel>
                                <ListView Name="LV_DwnPage"  SelectionChanged="LV_DwnPage_SelectionChanged" Height="427" Margin="0,0,-0.4,0" 
                                          ItemsSource="{Binding Source={StaticResource Bar}}">
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="小说名" Width="200">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Grid>
                                                            <TextBlock VerticalAlignment="Center" Text="{Binding  Path=Name}"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Header="进度" Width="700" >
                                                <GridViewColumn.CellTemplate >
                                                    <DataTemplate >
                                                        <Grid>
                                                            <ProgressBar Name="prs"  Value="{Binding Path=Barvalue}" Width="700" Maximum="100" Height="40"/>

                                                        </Grid>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                            <GridViewColumn Header=" " Width="100">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <WrapPanel>
                                                            <Button x:Name="dwn_ctrl" Content="暂停/开始" Style="{StaticResource Dwn_ctl_Btn}" Width="50"></Button>
                                                            <Button x:Name="dwn_del" Content="删除" Style="{StaticResource Dwn_del_Btn}" Width="50"></Button>
                                                        </WrapPanel>
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                            </GridViewColumn>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </StackPanel>

                        </TabItem>
                        <TabItem Header="已下载" IsSelected="True" FontSize="24" Width="200">
                            <Button Content="123456">
                            </Button>
                        </TabItem>
                        <TabItem Header="下载设置" IsSelected="True" FontSize="24" Width="200">
                            <Button Content="123456" Margin="537,0,255.8,0">
                            </Button>
                        </TabItem>
                    </TabControl>
                </Grid>
            </TabItem>
            <TabItem Header="书架" IsSelected="True" FontSize="24"  Width="255">
                <Grid x:Name="Boksf_grid">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                    </Grid.RowDefinitions>
                    <Button Content="boksf" Height="43" Width="91" FontSize="24" Margin="454,0,448.6,-22.2" Click="Button_Click_1"></Button>
                    <ListView x:Name="Boksf_lv" 
                              Background="#FFCEC4C4" 
                              SelectionChanged="Boksf_lv_SelectionChanged" 
                              Margin="0,43,-0.4,-660.2" RenderTransformOrigin="0.5,0.489"
                              ItemsSource="{Binding Source={StaticResource stu}}"
                              >
                        <ListView.View >
                            <GridView hc:BackgroundSwitchElement.MouseDownBackground="Black">
                                <GridViewColumn x:Name="fictionid" DisplayMemberBinding=
                          "{Binding Path=Id}"  Header="更新日期" Width="180"></GridViewColumn>
                                <GridViewColumn x:Name="fictionname" DisplayMemberBinding=
                          "{Binding Path=Name}" Header="小说名称" Width="200"></GridViewColumn>
                                <GridViewColumn x:Name="authorname" DisplayMemberBinding=
                          "{Binding Path=Age}" Header="作者名称" Width="200"></GridViewColumn>
                            </GridView>
                        </ListView.View>
                        <ListView.ContextMenu>
                            <ContextMenu >
                                <MenuItem Header="查看详情" Click="InfoPage"/>
                                <MenuItem Header="移除书架" Click="BookShelf"/>
                                <MenuItem Header="下载本书" Click="DownLoadBook"/>
                            </ContextMenu>
                        </ListView.ContextMenu>
                    </ListView>

                </Grid>
            </TabItem>
            <TabItem Header="设置" IsSelected="True" FontSize="24"  Width="255">
                <Grid>
                    <Grid Height="200"  VerticalAlignment="Top" Margin="0,0,-0.4,0">
                        <Label Content="阅读设置" Height="41" Width="135" FontSize="18" Margin="0,0,659,159" />
                    </Grid>
                    <Grid HorizontalAlignment="Left" Height="156" Margin="0,205,-0.4,0" VerticalAlignment="Top" Width="794">
                        <Label Content="下载设置" Width="136" Height="44" FontSize="18" Margin="0,0,658,106"></Label>
                        <TextBox Text="11" Width="100" Height="32" Margin="148,49,546,69" FontSize="14"/>
                        <Label Content="下载队列大小"  Width="132" Height="32" FontSize="15" Margin="0,49,662,69"/>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
        <hc:LoadingCircle Visibility="{Binding ShowProgress}" HorizontalAlignment="Center"  Foreground="BlueViolet" Grid.Row="3" Grid.ColumnSpan="4" RenderTransformOrigin="0.452,3.676" />
        <TextBlock x:Name="search_stat" Grid.Row="4" FontSize="30" Grid.Column="4" Grid.ColumnSpan="1"></TextBlock>
    </Grid>
</Window>
